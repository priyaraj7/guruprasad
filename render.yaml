services:
  - name: server
    plan: free
    type: web
    env: node
    repo: https://github.com/priyaraj7/guruprasad
    buildCommand: cd client && npm install && npm run build && cd ../server && npm install
    startCommand: cd server && node ./node_modules/db-migrate/bin/db-migrate up && node server.js
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: guruprasad
          property: connectionString

databases:
  - name: guruprasad
    plan: free
    ipAllowList: []
